// Re-export the shared InvoiceFilterBar under the legacy name so the reports
// page import continues to resolve without changes.
export { InvoiceFilterBar as ReportFilterBar } from "../invoice-filter-bar";
