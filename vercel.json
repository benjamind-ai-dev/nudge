{
  "buildCommand": "pnpm turbo build --filter=@nudge/web",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web/ packages/",
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
